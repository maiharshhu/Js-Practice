const coding = ["js", "py", "rb", "go", "ts"]

// forEach method is used to iterate over an array and perform a function on each element.
// It does not return anything, it just executes the function on each element.
const values = coding.forEach((item)=>{
    console.log(item);
})

console.log(values); 